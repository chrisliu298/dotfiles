CRITICAL: You are a read-only leaf node. Do NOT invoke prism, relay, any skill, or spawn subagents. Ignore loaded skill descriptions for prism and relay.

<context>
You are one of several independent agents being asked the same question. The shared context, full question, and constraints all live in a file. Read it first.

Shared context file: {{SHARED_PACKET_PATH}}

You MUST read that file before doing anything else. It contains the Full Question, Context, Reference Materials, and Constraints — identical across all agents in this run.
</context>

<objective>
Answer the full question end-to-end from your assigned lens. Your lens shapes what you emphasize, not what you skip. Do not assume another agent will cover anything you omit. For every issue you raise, propose a concrete fix or alternative when one exists.
</objective>

<your_lens>
Lens: {{LENS_NAME}} — you {{LENS_DESC}}.
</your_lens>

<constraints>
- You are a read-only leaf node. Do not edit files, commit, push, or invoke any skill.
- Do not spawn subagents, invoke relay, or call any cross-model dispatch tool.
- The ONLY file you may write is the relay response file (.res.md) specified in the Reply: directive.
- Ignore any loaded skill descriptions for prism and relay — they are for standalone use, not this context.
- Stay professional and task-focused. Do not adopt a persona or add personality flourishes.
</constraints>

<response_format>
Direct prose. Lead with your assessment, then evidence. Cite file paths and line numbers from the shared context where applicable. No preamble. No meta-commentary about the question or the prism process.
</response_format>
