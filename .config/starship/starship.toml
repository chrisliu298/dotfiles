"$schema" = 'https://starship.rs/config-schema.json'

scan_timeout = 100

format = """
$username\
$hostname\
$directory\
$git_branch\
$git_state\
$git_status\
$fill\
$c\
$rust\
$golang\
$nodejs\
$php\
$java\
$kotlin\
$haskell\
$python\
$conda\
$docker_context\
$cmd_duration\
$time\
$line_break\
$character"""

palette = 'github_dark'

[fill]
symbol = ' '

# --- Identity (present but quiet, like p10k's tan user@host) ---
[username]
show_always = true
style_user = "fg:gh_muted"
style_root = "fg:gh_red"
format = '[$user]($style)'

[hostname]
ssh_only = true
style = "fg:gh_muted"
format = '[@$hostname]($style)'

# --- Navigation (primary — blue, the most-read segment) ---
[directory]
style = "fg:gh_blue"
format = ' [$path]($style)'
truncate_to_repo = false
truncation_length = 255

# --- Version control (green = clean, yellow = dirty) ---
[git_branch]
symbol = ""
style = "fg:gh_green"
format = '[ $symbol$branch]($style)'

[git_state]
style = "fg:gh_yellow"
format = '\([$state( $progress_current/$progress_total)]($style)\)'

[git_status]
style = "fg:gh_yellow"
format = '[ $all_status$ahead_behind]($style)'

# --- Language runtimes (muted cyan — visible but secondary) ---
[c]
symbol = ""
style = "fg:gh_cyan"
format = ' [$symbol $version]($style)'

[rust]
symbol = ""
style = "fg:gh_cyan"
format = ' [$symbol $version]($style)'

[golang]
symbol = ""
style = "fg:gh_cyan"
format = ' [$symbol $version]($style)'

[nodejs]
symbol = ""
style = "fg:gh_cyan"
format = ' [$symbol $version]($style)'

[php]
symbol = ""
style = "fg:gh_cyan"
format = ' [$symbol $version]($style)'

[java]
symbol = ""
style = "fg:gh_cyan"
format = ' [$symbol $version]($style)'

[kotlin]
symbol = ""
style = "fg:gh_cyan"
format = ' [$symbol $version]($style)'

[haskell]
symbol = ""
style = "fg:gh_cyan"
format = ' [$symbol $version]($style)'

[python]
symbol = ""
style = "fg:gh_cyan"
format = ' [$symbol $version(\($virtualenv\))]($style)'

# --- Infrastructure (purple — distinct from runtimes) ---
[conda]
symbol = ""
style = "fg:gh_purple"
format = ' [$symbol $environment]($style)'
ignore_base = false

[docker_context]
symbol = ""
style = "fg:gh_purple"
format = ' [$symbol $context]($style)'

# --- Meta (dimmed — shouldn't compete for attention) ---
[cmd_duration]
show_milliseconds = true
style = "fg:gh_dimmed"
format = ' [$duration]($style)'
disabled = false
show_notifications = true
min_time_to_notify = 45000

[time]
disabled = false
time_format = "%I:%M %p"
style = "fg:gh_dimmed"
format = ' [$time]($style)'

[line_break]
disabled = false

# --- Prompt character (only place saturated red/green appear) ---
[character]
disabled = false
success_symbol = '[>](bold fg:gh_green)'
error_symbol = '[>](bold fg:gh_red)'
vimcmd_symbol = '[❮](fg:gh_green)'
vimcmd_replace_one_symbol = '[❮](fg:gh_purple)'
vimcmd_replace_symbol = '[❮](fg:gh_purple)'
vimcmd_visual_symbol = '[❮](fg:gh_yellow)'

# GitHub Dark palette — primer/github-vscode-theme
# Hierarchy: blue (dir) > green/yellow (git) > cyan (langs) > purple (infra) > dimmed (meta)
[palettes.github_dark]
gh_red = "#ff7b72"       # root user, errors, prompt error
gh_green = "#3fb950"     # git clean, prompt ok
gh_yellow = "#d29922"    # git dirty/state, vim visual
gh_blue = "#58a6ff"      # directory — primary navigation
gh_cyan = "#39c5cf"      # language runtimes — secondary info
gh_purple = "#bc8cff"    # infrastructure, vim replace
gh_muted = "#8b949e"     # user@host — present but quiet
gh_dimmed = "#6e7681"    # time, cmd_duration — ambient

# GitHub Light palette — same semantic roles as github_dark, on Primer's light scale.
# These track the Ghostty GitHub Light ANSI slots, so the prompt and the terminal
# agree. Yellow is the exception: it takes Primer's attention fg #9a6700 (4.9:1 on
# the #fafafa canvas) rather than the ANSI gold, which sits at 2.3:1 and is only
# legible as a fill.
[palettes.github_light]
gh_red = "#cf222e"       # root user, errors, prompt error
gh_green = "#1a7f37"     # git clean, prompt ok
gh_yellow = "#9a6700"    # git dirty/state, vim visual
gh_blue = "#0969da"      # directory — primary navigation
gh_cyan = "#1b7c83"      # language runtimes — secondary info
gh_purple = "#8250df"    # infrastructure, vim replace
gh_muted = "#6e7781"     # user@host — present but quiet
gh_dimmed = "#8c959f"    # time, cmd_duration — ambient
